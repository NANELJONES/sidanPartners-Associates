/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
        domains: ['ap-south-1.graphassets.com'], // Add the allowed domain here
      },
    
}


;

export default nextConfig;
